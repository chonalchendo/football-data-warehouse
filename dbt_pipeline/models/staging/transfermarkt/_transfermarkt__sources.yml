version: 2

sources:
  - name: transfermarkt
    tables:
      - name: squads
        meta:
          external_location: >
            read_json(
            '../data/raw/transfermarkt/*/squads.json.gz',
            auto_detect=true
            )
